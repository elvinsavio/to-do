package template

templ Heading(variant string, text string) {
	switch variant {
		case "heading":
			<h1 class="font-bold text-lg text-foreground">{ text }</h1>
		case "sub-heading":
			<h2 class="font-bold text-sm text-muted-foreground">{ text }</h2>
	}
}
